# Development archive

Dated design and session documents from git-lex's development. **Historical
record only — several describe machinery that was later redesigned or
deleted.** The user documentation lives one level up in `docs/`; the
current architecture reference is `ONE_GRAPH` there. Nothing in this
folder is a promise about how git-lex works today.
