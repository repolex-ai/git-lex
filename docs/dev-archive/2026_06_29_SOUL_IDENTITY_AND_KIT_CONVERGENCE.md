# Soul Identity & Convergent Kit Updates — MOVED

> **Canonical home:** `repolex-ai/subtexture/docs/2026_06_29_SOUL_IDENTITY_AND_KIT_CONVERGENCE.md`

This spec affects every soul (identity is squad-wide), so the canonical copy lives
in the **Subtexture specs repo** — the shared home for cross-component / squad-wide
contracts. This file is a greppable pointer so the doc is still discoverable from
git-lex (where the convergent-kit-update *implementation* lives).

**What's in it:** one canonical identity source per soul (root `SOUL.md`
frontmatter — the `soul:Soul` class was killed Day-44) re-derived into every
surface, plus a declarative convergent `kit-update` that reconciles a soul to the
kit's declared state (closes #90, the orphan hook registrations). The convergence
half is the build-ASAP part.

Edit the canonical copy in `subtexture/`, not this pointer.
